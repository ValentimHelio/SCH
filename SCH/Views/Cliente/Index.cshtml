@* @model IEnumerable<SCH.Models.Cliente> *@
@model ReflectionIT.Mvc.Paging.PagingList<SCH.Models.Cliente>

@{
    ViewData["Title"] = "Lista de Clientes";
}

<h1>Lista de Cliente(s)</h1>

<form method="get" class="form-inline">
    <div class="form-row align-items-center">
        <div class="row">
            <div class="col-sm-10 ">
                <input name="filter" class="form-control" placeholder="Pesquisar pelo nome do Cliente" value="@Model.RouteValue["Filter"]" />
            </div>
            <div class="col-auto">
                <button type="submit" class="btn btn-info"> Procurar</button>
            </div>
        </div>
    </div>
</form>

<br>
<p>
    <a asp-action="Create" class="btn btn-outline-info">Cadastar Novo Cliente</a>
</p>
<div class="container mt-3">
    <table class="table table-striped">
        <thead>
            <tr>
               @*  <th>
                    @Html.DisplayNameFor(model => model.ClienteId)
                </th> *@
                <th>
                    @Html.DisplayNameFor(model => model.NomeCliente)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Endereco)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Cidade)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Uf)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Cep)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Fone)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Cpf)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                   @*  <td>
                        @Html.DisplayFor(modelItem => item.ClienteId)
                    </td> *@
                    <td>
                        @Html.DisplayFor(modelItem => item.NomeCliente)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Endereco)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cidade)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Uf)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cep)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Fone)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cpf)
                    </td>
                    <td>
                        <a asp-action="Edit" class="btn btn-outline-primary" asp-route-id="@item.ClienteId">Editar</a>
                        <a asp-action="Details" class="btn btn-outline-secondary" asp-route-id="@item.ClienteId">Detalhes</a>
                        <a asp-action="Delete" class="btn btn-outline-danger" asp-route-id="@item.ClienteId">Excluir</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    Total de Cliente(s) : @Model.TotalRecordCount
    <div>
        <vc:pager paging-list="@Model"></vc:pager>
    </div>
</div>